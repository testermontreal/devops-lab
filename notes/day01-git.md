# Day 01 — Git Foundations

## Ключевые концепции / Key Concepts

| RU термин | EN термин | Объяснение |
|-----------|-----------|-----------|
| Область подготовки | Staging Area / Index | Промежуточная зона перед коммитом |
| Рабочая директория | Working Directory | Файлы как они есть на диске |
| Снимок | Snapshot | Состояние всех файлов в момент коммита |
| Указатель | Pointer / Reference | HEAD, ветки — это просто указатели на SHA |
| Слияние | Merge | Объединение истории двух веток |
| Перебазирование | Rebase | Перенос коммитов поверх другой базы |
| Форк | Fork | Копия чужого репо под своим аккаунтом |

## Команды

| Команда | Что делает |
|---------|-----------|
| `git add -p` | Интерактивный staging по кускам (patch mode) |
| `git diff --staged` | Diff между staging и последним коммитом |
| `git log --oneline --graph --all` | Граф истории всех веток |
| `git stash` | Спрятать незакоммиченные изменения во временный стек |
| `git restore --staged <file>` | Убрать файл из staging (не удаляет изменения) |
| `git show HEAD` | Показать содержимое последнего коммита |

## Bug DVPS-4: Permission denied (publickey)
**Симптом:** `git push` выдаёт `ERROR: Permission denied (publickey)`
**Причина:** remote URL использует HTTPS, а не SSH
**Диагностика:** `git remote -v` → видим `https://` вместо `git@`
**Fix:** `git remote set-url origin git@github.com:user/repo.git`
**Проверка:** `git remote -v` → должен показывать `git@`

###################################################################################

## Квиз Дня 1

Запиши ответы в notes/day01-git.md своими словами. Не гугли — пробелы нужны нам обоим для адаптации плана.


1. Объясни своими словами: зачем существует staging area (область подготовки)? Почему нельзя сразу git commit?

Область staging area Существует качестве срединной области между working area И local repository.
Поэтому мы сначала переносим с помощью команды git add, file is working directory, the staging area, а потом уже с помощью команды git commit -m "message" мы переносим из staging area в local repository.

2. Что произойдёт если запустить git commit без предварительного git add?
Я точно не знаю ответа, поэтому буду логически размышлять.
Если мы предварительно не делали команды git add?
То is working area. Ничего не переместилось в staging area.
Соответственно, если мы сделаем команды git commit без предварительного git add 
То нечего будет перемещать из staging area в local repo. И должна появиться ошибка.

3. Чем отличается git reset HEAD~1 от git revert HEAD? Когда безопасно использовать каждый?
Я точно не знаю ответа, поэтому буду логически размышлять.
Разбираю первую команду git reset HEAD TO1.
Здесь мы делаем Перезагрузку, то есть reset Head Первый переменный. А во втором случае, во второй команде мы Возвращаем HEAD.

4. Ты сделал git add secret.env, затем добавил *.env в .gitignore. Файл всё равно попадёт в следующий коммит — почему и как исправить?
Точно не знаю, ответ на вопрос, поэтому буду думать с логически.
Если я сделаю команду git add secrets.env 
То этот файл переместится из рабочей директории в staging area.
Исправить это следующим образом можно.
Нам нужно удовить этот файл из staging area и только после этого делась команду git commit.

5. Что хранится в .git/objects/? Назови 4 типа Git-объектов и что каждый представляет.
Я точно не знаю ответа, поэтому буду логически размышлять.
Думаю, что в этой директории хранятся структура проекта и sha.
Четыре типа объектов: blob (содержимое файла), tree (директория), commit (снимок + метаданные), tag (аннотированный тег).
Вообще, эта тема очень сложная, хотя бы какую-то простую. В структуру в виде рисунка или таблица, где всё это просто объяснину.

6. Почему heredoc с кавычками << 'EOF' предпочтительнее << EOF при создании конфигов?
Я точно не знаю ответа, поэтому буду логически размышлять.
Данная структура указывает на начало и конец кода, который мы передаем в файл.
Если я не ошибаюсь, EOF расшифровывается как «and-of-file». 
Почему с кавычками я точно не помню, но кажется, это как-то связано.
Чёрт, забыл, напомни, пожалуйста, мне простыми словами.

7. Ты работаешь в команде. Коллега сделал git push --force в main. Что произошло и почему это катастрофа?
Я точно не знаю ответа, поэтому буду логически размышлять.
Если мой коллега сделал эту команду git push --force.
Значит, он стёр то, что было раньше. Не оставил от этого никаких следов и сверху записал ту информацию, которую запушил из своего локального репозитория. Таким образом, это катастрофа, потому что мы потеряли прошлые данные.
Если я не прав, объясни, каким это более подробными простыми словами.

8. Чем Ed25519 лучше RSA-4096 для SSH-ключей? Назови 2 конкретных преимущества.
Я понял, что современный стандарт Ed25519 лучше, чем RSA-4096 для SSH-ключей, тем, что он более современный, быстрый и безопасный, а еще у него публичный ключ короче.

###########################################################################################

## Bug DVPS-8: AMDGPU Errors in System Logs

**Severity Assessment (оценка критичности):**
- Desktop: WARN (не мешает работе)
- Production GPU server: CRITICAL

**Root Cause (корневая причина):**
- page fault = GPU пытается обратиться к памяти, которая уже освобождена
- Обычно виноват Firefox или Chromium (GPU-accelerated rendering)

**Impact:** non-critical on desktop, visual glitches possible

**Mitigation (временное решение):**
- Отключить GPU-ускорение в браузере
- Обновить драйвер: sudo apt install linux-firmware

**Status:** Known issue, monitoring only
